{
  "name": "team-task-manager-root",
  "private": true,
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "preinstall": "npx -y check-node-version --node \">=18.0.0\"",
    "install-server": "npm install --prefix server",
    "install-client": "npm install --prefix client",
    "install-all": "npm run install-server && npm run install-client",
    "build": "npm run install-all && cd server && npm run build:client",
    "start": "cd server && npm start",
    "start:prod": "npm run install-all && cd server && npm run build:client && npm start"
  }
}
